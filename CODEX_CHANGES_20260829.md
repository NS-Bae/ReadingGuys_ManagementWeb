# 관리자 웹 개편 적용 안내

## 주요 변경

- 관리자 로그인과 세션 만료 처리를 일관된 방식으로 정리했습니다.
- 학원, 회원, 문제집, 약관·앱 정보 화면을 반응형 대시보드로 교체했습니다.
- 교사용 내 학원 및 학생 학습 기록 화면을 교체했습니다.
- API 오류 메시지, 로딩, 빈 목록, 재시도, 작업 완료 상태를 화면에 표시합니다.
- 문제집 업로드에 초급/중급/고급과 5개 독해 분야를 반영했습니다.
- 약관 문서 등록, Markdown 미리보기, 버전 이력, 본문 확인, 활성화를 지원합니다.

## 실행과 배포

1. `npm ci`
2. `npm run build`
3. 기존 GitHub Actions를 이용해 `main` 브랜치에 배포

API 기본 경로는 `/api`입니다. 다른 서버 주소를 사용할 때만 빌드 환경 변수 `REACT_APP_API_BASE_URL`을 지정하세요.
로컬 `npm start`에서는 개발 프록시가 `/api` 요청을 `https://adminreadingguys.com`으로 전달하므로 별도의 CORS 설정 없이 운영 API를 시험할 수 있습니다. `package.json`의 `proxy` 변경 후에는 개발 서버를 반드시 다시 시작해야 합니다.

이 웹 수정본은 함께 제공한 Nest 서버 수정본의 약관 목록/본문 API와 확장된 문제집 목록 응답을 사용합니다. 서버를 먼저 배포한 뒤 웹을 배포하는 것을 권장합니다.
